# scripts/

Developer and release scripts for the Signoff monorepo.

**Phase 0 placeholder.** Populated as the need arises — the first real
scripts land alongside the core data models (schema export) in the next PR.
