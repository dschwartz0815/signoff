# signoff-mcp

MCP server adapter for [Signoff](../../README.md).

**Phase 0 scaffolding.** Exposes no tools yet; the `request_signoff`, `list_verifiers`, and `get_verdict` tools arrive in follow-up PRs per [`docs/protocol.md`](../../docs/protocol.md) §7.3.
