// Signoff TypeScript SDK.
//
// Phase 0 scaffolding. Protocol types and HTTP/MCP clients arrive in
// follow-up PRs alongside the Python core. Keep the public surface empty
// until those land so consumers cannot take accidental dependencies.

export const VERSION = '0.0.1';
