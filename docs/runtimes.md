# Runtimes

**Status:** Phase 0 stub. The full guide lands in Phase 1 with the `signoff-runtime-docker` package.

Normative contract: [`CLAUDE.md`](../CLAUDE.md) §8. Covers the `Runtime` protocol, `LocalRuntime` defaults, `DockerRuntime` policy (CPU/memory/disk/network limits, cosign verification), and sandbox image conventions.
