# Writing a Pack

**Status:** Phase 0 stub. The full guide lands alongside the first shipped pack (`signoff-code`, Phase 1).

Repo-level conventions: [`CLAUDE.md`](../CLAUDE.md) §11. Every pack ships with a `default_config.yaml`, a README documenting every verifier, and a regression suite with realistic claim + ground-truth pairs.
